# Investment Research Agent — runtime dependencies.
#
# Self-contained: includes the MCP server's own dependencies because ADK
# launches mcp_server/server.py as a stdio subprocess inside the same container.

# --- Google ADK + Gemini + A2A protocol ---
google-adk>=1.0.0
google-genai>=1.0.0
a2a-sdk>=0.2.0
uvicorn[standard]>=0.30.0

# --- MCP server (spawned as a subprocess) ---
neo4j>=5.20.0
mcp[cli]>=1.2.0
python-dotenv>=1.0.0
